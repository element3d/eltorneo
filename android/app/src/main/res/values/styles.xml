<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowBackground">@color/splash_background_color</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_screen</item>
        <item name="android:colorControlActivated">#69b7ff</item>
        <item name="android:textColorHighlight">#afd7fd</item>
                <item name="android:windowIsTranslucent">true</item>

    </style>
 
</resources>
